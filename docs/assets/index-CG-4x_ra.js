(function(){const s=document.createElement("link").relList;if(s&&s.supports&&s.supports("modulepreload"))return;for(const o of document.querySelectorAll('link[rel="modulepreload"]'))a(o);new MutationObserver(o=>{for(const n of o)if(n.type==="childList")for(const r of n.addedNodes)r.tagName==="LINK"&&r.rel==="modulepreload"&&a(r)}).observe(document,{childList:!0,subtree:!0});function t(o){const n={};return o.integrity&&(n.integrity=o.integrity),o.referrerPolicy&&(n.referrerPolicy=o.referrerPolicy),o.crossOrigin==="use-credentials"?n.credentials="include":o.crossOrigin==="anonymous"?n.credentials="omit":n.credentials="same-origin",n}function a(o){if(o.ep)return;o.ep=!0;const n=t(o);fetch(o.href,n)}})();const C="2.8.0",T="swimtimer-independent-v2",G=["Anna","Ben","Chloe","David","Eva","Frank","Grace","Henry","Ivy","Jack"],I=e=>{const s=crypto.randomUUID();return{id:s,studentId:s,name:e,status:"idle",elapsed:0,startedAt:null,laps:[],distance:0,lastSavedSignature:""}},k={timers:G.map(I),view:"grid",settings:{vibration:!0,keepAwake:!0,poolLength:20},sessions:[]};function M(e){return String(e||"Unnamed").trim().toLocaleLowerCase()}function j(){try{const e=JSON.parse(localStorage.getItem(T));if(!e?.timers?.length)return structuredClone(k);const s={...structuredClone(k),...e,settings:{...k.settings,...e.settings},sessions:Array.isArray(e.sessions)?e.sessions:[]};s.timers=s.timers.map(o=>({...I(o.name||"Swimmer"),...o,studentId:o.studentId||o.id||crypto.randomUUID(),lastSavedSignature:o.lastSavedSignature||""}));const t=new Map(s.timers.map(o=>[M(o.name),o.studentId])),a=new Map;return s.sessions=s.sessions.map(o=>({...o,timers:(o.timers||[]).map(n=>{const r=M(n.name);return a.has(r)||a.set(r,`legacy-${r||crypto.randomUUID()}`),{...n,studentId:n.studentId||t.get(r)||a.get(r),stroke:n.stroke||"",test:n.test||"",equipment:n.equipment||"",notes:n.notes||""}})})),s}catch{return structuredClone(k)}}let i=j(),O=0,L=null;const W=document.querySelector("#app"),p=()=>localStorage.setItem(T,JSON.stringify(i)),v=(e,s=Date.now())=>e.elapsed+(e.status==="running"?s-e.startedAt:0),g=()=>Math.max(1,Number(i.settings.poolLength)||20),h=e=>Math.max(0,Number(e.distance)||e.laps.length*g()),b=(e,s)=>Math.max(0,Number(e.distance)||e.laps.length*s.poolLength),l=e=>String(e).replace(/[&<>'"]/g,s=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"})[s]),S=e=>new Intl.DateTimeFormat("en-AU",{dateStyle:"medium",timeStyle:"short"}).format(e),B=e=>`${Math.round(v(e))}|${h(e)}|${e.laps.length}`,P=e=>!!(e.lastSavedSignature&&e.lastSavedSignature===B(e));function u(e){const s=Math.max(0,Math.floor(e/10)),t=s%100,a=Math.floor(s/100),o=a%60,n=Math.floor(a/60);return`${String(n).padStart(2,"0")}:${String(o).padStart(2,"0")}.${String(t).padStart(2,"0")}`}const X='<svg viewBox="0 0 24 24" fill="none" stroke="currentColor"><circle cx="12" cy="12" r="3"/><path d="M19 12a7 7 0 0 0-.1-1l2-1.5-2-3.4-2.4 1A7 7 0 0 0 15 6l-.3-2.6h-4L10.5 6A7 7 0 0 0 9 7.1l-2.4-1-2 3.4 2 1.5a7 7 0 0 0 0 2l-2 1.5 2 3.4 2.4-1A7 7 0 0 0 10.5 18l.3 2.6h4L15 18a7 7 0 0 0 1.5-1.1l2.4 1 2-3.4-2-1.5a7 7 0 0 0 .1-1Z"/></svg>';function m(){W.innerHTML=`<main class="app-shell">
    <header class="topbar">
      <div><h1>Swim<span>Timer</span></h1><p>Independent timers</p></div>
      <div class="header-actions">
        <div class="view-toggle" role="group" aria-label="Timer layout">
          <button data-view="grid" class="${i.view==="grid"?"active":""}">Grid</button>
          <button data-view="list" class="${i.view==="list"?"active":""}">List</button>
        </div>
        <button class="settings-button" id="settings" aria-label="Manage timers and settings">${X}</button>
      </div>
    </header>
    <section class="timer-grid ${i.view}" aria-label="Independent swimmer timers">
      ${i.timers.map((e,s)=>Y(e,s)).join("")}
    </section>
    <footer class="global-rail">
      <button class="all-start" id="all-start">ALL START</button>
      <button class="all-stop" id="all-stop">ALL STOP</button>
      <button class="all-reset" id="all-reset">ALL RESET</button>
    </footer>
    <dialog id="dialog"></dialog>
  </main>`,document.querySelectorAll("[data-view]").forEach(e=>e.addEventListener("click",()=>{i.view=e.dataset.view,p(),m()})),document.querySelectorAll("[data-name]").forEach(e=>e.addEventListener("click",()=>te(e.dataset.name))),document.querySelectorAll("[data-laps]").forEach(e=>e.addEventListener("click",()=>se(e.dataset.laps))),document.querySelectorAll("[data-distance]").forEach(e=>e.addEventListener("click",()=>ae(e.dataset.distance))),document.querySelectorAll("[data-action]").forEach(e=>e.addEventListener("click",()=>K(e.dataset.id,e.dataset.action))),document.querySelector("#all-start").addEventListener("click",Z),document.querySelector("#all-stop").addEventListener("click",Q),document.querySelector("#all-reset").addEventListener("click",ee),document.querySelector("#settings").addEventListener("click",f),z()}function Y(e,s){const t=e.laps.at(-1),a=e.status==="running",n=v(e)>0||e.laps.length>0||h(e)>0,r=P(e);return`<article class="timer-card ${a?"running":""}" data-card="${e.id}">
    <div class="card-head"><span class="lane-number">${s+1}</span><button class="timer-name" data-name="${e.id}" aria-label="Edit ${l(e.name)}">${l(e.name)}</button><span class="status-dot" aria-label="${a?"Running":e.status==="paused"?"Stopped":"Idle"}"></span></div>
    <div class="elapsed" data-clock="${e.id}">${u(v(e))}</div>
    <div class="timer-meta"><span class="meta-block"><small>Latest</small><b>${t?u(t.split):"00:00.00"}</b></span><button class="meta-block lap-summary" data-laps="${e.id}" aria-label="View ${l(e.name)} lap times" ${e.laps.length?"":"disabled"}><small>Laps</small><b>${e.laps.length}</b></button><button class="meta-block distance-summary" data-distance="${e.id}" aria-label="Set ${l(e.name)} distance"><small>Distance</small><b>${h(e)}m</b></button></div>
    <div class="card-actions">
      <button class="${a?"stop-button":"start-button"}" data-action="${a?"stop":"start"}" data-id="${e.id}">${a?"STOP":"START"}</button>
      <button class="${a?"lap-button":"reset-button"}" data-action="${a?"lap":"reset"}" data-id="${e.id}" ${!a&&!n?"disabled":""}>${a?"LAP":"RESET"}</button>
      <button class="save-button ${r?"saved":""}" data-action="save" data-id="${e.id}" ${a||!n||r?"disabled":""}>${r?"SAVED":"SAVE"}</button>
    </div>
  </article>`}function z(){cancelAnimationFrame(O);const e=()=>{const s=Date.now();let t=!1;for(const a of i.timers){if(a.status!=="running")continue;t=!0;const o=document.querySelector(`[data-clock="${a.id}"]`);o&&(o.textContent=u(v(a,s)))}t&&(O=requestAnimationFrame(e))};e()}function K(e,s){const t=i.timers.find(a=>a.id===e);if(t){if(s==="start"&&(t.startedAt=Date.now(),t.status="running",t.lastSavedSignature="",x()),s==="stop"&&t.status==="running"&&(t.elapsed=v(t),t.startedAt=null,t.status="paused"),s==="lap"&&t.status==="running"){const a=v(t);t.laps.push({at:a,split:a-(t.laps.at(-1)?.at||0)}),t.lastSavedSignature="",i.settings.vibration&&navigator.vibrate&&navigator.vibrate(35)}if(s==="reset"){U(t),i.timers.some(a=>a.status==="running")||w(),p(),m();return}if(s==="save"){de(t);return}i.timers.some(a=>a.status==="running")||w(),p(),m()}}function Z(){const e=Date.now();i.timers.forEach(s=>{s.status!=="running"&&(s.startedAt=e,s.status="running")}),x(),p(),m()}function Q(){const e=Date.now();i.timers.forEach(s=>{s.status==="running"&&(s.elapsed=v(s,e),s.startedAt=null,s.status="paused")}),w(),p(),m()}function U(e){Object.assign(e,{status:"idle",elapsed:0,startedAt:null,laps:[],distance:0,lastSavedSignature:""})}function ee(){const e=document.querySelector("#dialog");e.innerHTML='<h2>Reset all timers?</h2><p>This removes every current time and all recorded laps.</p><div class="dialog-actions"><button class="dialog-secondary" data-close>Cancel</button><button class="dialog-danger" id="confirm-reset">Reset</button></div>',e.open||e.showModal(),e.querySelector("[data-close]").addEventListener("click",()=>e.close()),e.querySelector("#confirm-reset").addEventListener("click",()=>{i.timers.forEach(U),i.timers.some(s=>s.status==="running")||w(),p(),e.close(),m()})}function te(e){const s=i.timers.find(o=>o.id===e);if(!s)return;const t=document.querySelector("#dialog");t.innerHTML=`<form><h2>Swimmer name</h2><label class="sr-only" for="name-input">Swimmer name</label><input id="name-input" class="text-input" maxlength="24" value="${l(s.name)}"><div class="dialog-actions"><button type="button" class="dialog-secondary" data-close>Cancel</button><button class="dialog-primary">Save</button></div></form>`,t.showModal();const a=t.querySelector("input");a.select(),t.querySelector("[data-close]").addEventListener("click",()=>t.close()),t.querySelector("form").addEventListener("submit",o=>{o.preventDefault();const n=a.value.trim();n&&(s.name=n,i.sessions.forEach(r=>r.timers.forEach(c=>{c.studentId===s.studentId&&(c.name=n)})),p(),t.close(),m())})}function se(e){const s=i.timers.find(a=>a.id===e);if(!s?.laps.length)return;const t=document.querySelector("#dialog");t.innerHTML=`<h2>${l(s.name)} — Lap times</h2>
    <p class="dialog-subtitle">${g()}m per lap · ${s.laps.length*g()}m total</p>
    <div class="lap-list" role="list">
      ${s.laps.map((a,o)=>`<div class="lap-row" role="listitem"><strong>Lap ${o+1}<small>${(o+1)*g()}m</small></strong><span><small>Lap time</small><b>${u(a.split)}</b></span><span><small>Total</small><b>${u(a.at)}</b></span></div>`).join("")}
    </div>
    <div class="dialog-actions"><button class="dialog-secondary" data-close>Done</button></div>`,t.showModal(),t.querySelector("[data-close]").addEventListener("click",()=>t.close())}function ae(e){const s=i.timers.find(n=>n.id===e);if(!s)return;const t=document.querySelector("#dialog"),a=[...new Set([g(),g()*2,50,100,200,400,800])].sort((n,r)=>n-r);t.innerHTML=`<form><h2>${l(s.name)} — Distance</h2><p>Choose a common distance or enter a custom total. LAP is optional.</p><div class="distance-presets">${a.map(n=>`<button type="button" data-distance-preset="${n}">${n}m</button>`).join("")}</div><label class="distance-entry"><span>Custom distance</span><span><input id="distance-input" type="number" min="0" max="100000" step="1" inputmode="numeric" value="${h(s)}"><b>m</b></span></label><div class="dialog-actions"><button type="button" class="dialog-secondary" data-close>Cancel</button><button class="dialog-primary">Save distance</button></div></form>`,t.showModal();const o=t.querySelector("#distance-input");o.select(),t.querySelectorAll("[data-distance-preset]").forEach(n=>n.addEventListener("click",()=>{o.value=n.dataset.distancePreset,t.querySelectorAll("[data-distance-preset]").forEach(r=>r.classList.toggle("active",r===n))})),t.querySelector("[data-close]").addEventListener("click",()=>t.close()),t.querySelector("form").addEventListener("submit",n=>{n.preventDefault(),s.distance=Math.min(1e5,Math.max(0,Math.round(Number(o.value)||0))),s.lastSavedSignature="",p(),t.close(),m()})}function f(){const e=document.querySelector("#dialog"),s=i.timers.some(a=>v(a)>0||a.laps.length||h(a)>0),t=E();e.innerHTML=`<h2>Manage timers</h2>
    <div class="manage-count"><button id="remove-timer" aria-label="Remove last timer">−</button><strong>${i.timers.length} swimmers</strong><button id="add-timer" aria-label="Add timer">＋</button></div>
    <div class="setting-row"><label for="pool-length">Pool length</label><span class="distance-input"><input id="pool-length" type="number" min="1" max="500" step="1" inputmode="numeric" value="${g()}"><b>m</b></span></div>
    <div class="setting-row"><span>Vibrate on lap</span><button class="switch ${i.settings.vibration?"on":""}" data-setting="vibration" aria-label="Toggle vibration"></button></div>
    <div class="setting-row"><span>Keep screen awake</span><button class="switch ${i.settings.keepAwake?"on":""}" data-setting="keepAwake" aria-label="Toggle screen wake lock"></button></div>
    <div class="records-actions"><button class="save-records" id="save-session" ${s?"":"disabled"}>Save current session</button><button class="view-records" id="view-sessions">Saved sessions <b>${i.sessions.length}</b></button><button class="student-records" id="view-students">Student records <b>${t.length}</b></button></div>
    <div class="data-actions"><button id="export-backup">Backup JSON</button><button id="export-csv">Export CSV</button><button id="import-backup">Restore backup</button><input class="sr-only" id="backup-file" type="file" accept="application/json,.json"></div>
    <p class="settings-tip">Tap a swimmer name to rename their profile. Previous records remain connected.</p>
    <p class="app-version">Version ${C}</p>
    <div class="dialog-actions"><button class="dialog-secondary" data-close>Done</button></div>`,e.open||e.showModal(),e.querySelector("#add-timer").addEventListener("click",()=>{i.timers.length>=30||(i.timers.push(I(`Swimmer ${i.timers.length+1}`)),p(),f())}),e.querySelector("#remove-timer").addEventListener("click",()=>{i.timers.length<=1||(i.timers.pop(),i.timers.some(a=>a.status==="running")||w(),p(),f())}),e.querySelector("#pool-length").addEventListener("change",a=>{i.settings.poolLength=Math.min(500,Math.max(1,Math.round(Number(a.target.value)||20))),p(),m(),f()}),e.querySelector("#save-session").addEventListener("click",le),e.querySelector("#view-sessions").addEventListener("click",D),e.querySelector("#view-students").addEventListener("click",N),e.querySelector("#export-backup").addEventListener("click",ne),e.querySelector("#export-csv").addEventListener("click",ie),e.querySelector("#import-backup").addEventListener("click",()=>e.querySelector("#backup-file").click()),e.querySelector("#backup-file").addEventListener("change",re),e.querySelectorAll("[data-setting]").forEach(a=>a.addEventListener("click",()=>{const o=a.dataset.setting;i.settings[o]=!i.settings[o],p(),f()})),e.querySelector("[data-close]").addEventListener("click",()=>e.close())}function F(e,s,t){const a=document.createElement("a");a.href=URL.createObjectURL(new Blob([s],{type:t})),a.download=e,document.body.append(a),a.click(),a.remove(),setTimeout(()=>URL.revokeObjectURL(a.href),1e3)}function ne(){const e=new Date().toISOString().slice(0,10);F(`swimtimer-backup-${e}.json`,JSON.stringify({app:"SwimTimer",version:C,exportedAt:Date.now(),data:i},null,2),"application/json")}function oe(e){return`"${String(e??"").replaceAll('"','""')}"`}function ie(){const e=[["Student","Distance (m)","Time","Milliseconds","Date","Stroke","Test / Set","Equipment","Notes","Laps"]];for(const s of i.sessions)for(const t of s.timers)e.push([t.name,b(t,s),u(t.elapsed),Math.round(t.elapsed||0),new Date(s.savedAt).toISOString(),t.stroke||"",t.test||"",t.equipment||"",t.notes||"",t.laps.length]);F(`swimtimer-records-${new Date().toISOString().slice(0,10)}.csv`,`\uFEFF${e.map(s=>s.map(oe).join(",")).join(`
`)}`,"text/csv;charset=utf-8")}async function re(e){const s=e.target.files?.[0];if(s)try{const t=JSON.parse(await s.text()),a=t.data||t;if(!Array.isArray(a.timers)||!Array.isArray(a.sessions))throw new Error("Invalid backup");if(!window.confirm("Replace current timers and records with this backup?")){e.target.value="";return}localStorage.setItem(T,JSON.stringify(a)),i=j(),m(),f()}catch{window.alert("This backup file could not be restored."),e.target.value=""}}function le(){const e=Date.now(),s=i.timers.map(t=>({studentId:t.studentId,name:t.name,elapsed:v(t,e),distance:h(t),laps:t.laps.map(a=>({...a})),stroke:"",test:"",equipment:"",notes:""})).filter(t=>t.elapsed>0||t.laps.length||t.distance>0);s.length&&(i.sessions.unshift({id:crypto.randomUUID(),savedAt:e,poolLength:g(),timers:s}),i.sessions=i.sessions.slice(0,100),p(),D())}function ce(e,s){return i.sessions.flatMap(t=>t.timers.map(a=>({session:t,timer:a}))).filter(t=>t.timer.studentId===e&&b(t.timer,t.session)===s&&t.timer.elapsed>0).sort((t,a)=>a.session.savedAt-t.session.savedAt)}function de(e){if(P(e))return;const s=document.querySelector("#dialog");s.innerHTML=`<form id="result-form"><h2>Save ${l(e.name)}</h2><div class="record-hero compact"><strong>${h(e)}m</strong><b>${u(v(e))}</b></div>
    <div class="record-fields"><label>Stroke<select id="result-stroke"><option value="">Not specified</option><option>Freestyle</option><option>Backstroke</option><option>Breaststroke</option><option>Butterfly</option><option>Individual medley</option></select></label><label>Test / set<input id="result-test" maxlength="40" placeholder="e.g. Time trial"></label><label>Equipment<input id="result-equipment" maxlength="40" placeholder="e.g. Fins, paddles"></label><label class="wide">Notes<textarea id="result-notes" maxlength="200" rows="3" placeholder="Optional coaching notes"></textarea></label></div>
    <div class="dialog-actions"><button type="button" class="dialog-secondary" data-close>Cancel</button><button class="dialog-primary">Save record</button></div></form>`,s.showModal(),s.querySelector("[data-close]").addEventListener("click",()=>s.close()),s.querySelector("#result-form").addEventListener("submit",t=>{t.preventDefault(),ue(e,{stroke:s.querySelector("#result-stroke").value,test:s.querySelector("#result-test").value.trim(),equipment:s.querySelector("#result-equipment").value.trim(),notes:s.querySelector("#result-notes").value.trim()})})}function ue(e,s={}){const t=Date.now(),a=B(e);if(e.lastSavedSignature===a)return;const o=h(e),n=ce(e.studentId,o),r=n[0]?.timer.elapsed||0,c=n.reduce((A,_)=>Math.min(A,_.timer.elapsed),Number.POSITIVE_INFINITY),d={studentId:e.studentId,name:e.name,elapsed:v(e,t),distance:o,laps:e.laps.map(A=>({...A})),...s};if(d.elapsed<=0&&d.distance<=0&&!d.laps.length)return;i.sessions.unshift({id:crypto.randomUUID(),savedAt:t,poolLength:g(),individual:!0,timers:[d]}),i.sessions=i.sessions.slice(0,100),e.lastSavedSignature=a,p();const y=document.querySelector("#dialog"),R=d.elapsed>0&&d.elapsed<c,q=r>0?d.elapsed-r:0,J=r?R?`New PB · ${u(Math.abs(c-d.elapsed))} faster`:`${q<=0?"▲":"▼"} ${u(Math.abs(q))} ${q<=0?"faster":"slower"} than last`:"First result at this distance";y.innerHTML=`<h2>${l(e.name)} saved</h2><div class="record-hero"><strong>${d.distance}m</strong><b>${u(d.elapsed)}</b></div><p class="performance-note ${R?"pb":""}">${J}</p><div class="dialog-actions"><button class="dialog-secondary" data-close>Done</button><button class="dialog-primary" id="view-saved-student">View record</button></div>`,y.showModal(),y.querySelector("[data-close]").addEventListener("click",()=>{y.close(),m()}),y.querySelector("#view-saved-student").addEventListener("click",()=>$(e.studentId))}function D(){const e=document.querySelector("#dialog");e.innerHTML=`<h2>Saved sessions</h2>
    <div class="session-list">
      ${i.sessions.length?i.sessions.map(s=>{const t=s.timers.reduce((a,o)=>a+b(o,s),0);return`<button class="session-card" data-session="${s.id}"><strong>${S(s.savedAt)}</strong><span>${s.timers.length} swimmers · ${t}m recorded</span></button>`}).join(""):'<p class="empty-state">No saved sessions yet.</p>'}
    </div>
    <div class="dialog-actions"><button class="dialog-secondary" id="records-back">Back</button></div>`,e.open||e.showModal(),e.querySelectorAll("[data-session]").forEach(s=>s.addEventListener("click",()=>H(s.dataset.session))),e.querySelector("#records-back").addEventListener("click",f)}function E(){const e=new Map,s=new Map(i.timers.map(t=>[t.studentId,t.name]));for(const t of i.sessions)t.timers.forEach((a,o)=>{const n=a.studentId||`legacy-${M(a.name)}`;e.has(n)||e.set(n,{key:n,name:s.get(n)||a.name||"Unnamed",results:[]}),e.get(n).results.push({session:t,timer:a,timerIndex:o})});return[...e.values()].map(t=>({...t,results:t.results.sort((a,o)=>o.session.savedAt-a.session.savedAt)})).sort((t,a)=>t.name.localeCompare(a.name))}function N(){const e=E(),s=document.querySelector("#dialog");s.innerHTML=`<h2>Student records</h2><p class="dialog-subtitle">All saved results grouped by student.</p>
    <div class="student-list">
      ${e.length?e.map(t=>{const a=t.results.reduce((n,r)=>n+b(r.timer,r.session),0),o=new Set(t.results.filter(n=>n.timer.elapsed>0).map(n=>b(n.timer,n.session))).size;return`<button class="student-card" data-student="${l(t.key)}"><strong>${l(t.name)}</strong><span>${t.results.length} record${t.results.length===1?"":"s"} · ${o} distance${o===1?"":"s"} · ${a}m total</span></button>`}).join(""):'<p class="empty-state">Save a session to create student records.</p>'}
    </div>
    <div class="dialog-actions"><button class="dialog-secondary" id="students-back">Back</button></div>`,s.open||s.showModal(),s.querySelectorAll("[data-student]").forEach(t=>t.addEventListener("click",()=>$(t.dataset.student))),s.querySelector("#students-back").addEventListener("click",f)}function $(e){const s=E().find(n=>n.key===e);if(!s)return;const t=new Map;for(const n of s.results){const r=b(n.timer,n.session);t.has(r)||t.set(r,[]),t.get(r).push(n)}const a=[...t.entries()].sort(([n],[r])=>n-r).map(([n,r])=>({distance:n,results:r.sort((c,d)=>(c.timer.elapsed||Number.MAX_SAFE_INTEGER)-(d.timer.elapsed||Number.MAX_SAFE_INTEGER)||d.session.savedAt-c.session.savedAt)})),o=document.querySelector("#dialog");o.innerHTML=`<h2>${l(s.name)}</h2><p class="dialog-subtitle">Sorted by distance · fastest time first</p>
    <div class="student-results">
      ${a.map(n=>`<section class="distance-record-group"><h3><strong>${n.distance}m</strong><span>${n.results.length} record${n.results.length===1?"":"s"}</span></h3><div class="distance-results">${n.results.map((r,c)=>`<div class="student-result-row"><button class="student-result" data-result-session="${r.session.id}" data-result-timer="${r.timerIndex}"><span class="result-title"><strong class="result-time">${u(r.timer.elapsed)}</strong>${c===0&&r.timer.elapsed>0?"<em>PB</em>":""}</span><small>${S(r.session.savedAt)} · ${r.timer.stroke?`${l(r.timer.stroke)} · `:""}${r.timer.laps.length} laps</small></button><button class="delete-record" data-delete-session="${r.session.id}" data-delete-timer="${r.timerIndex}" aria-label="Delete ${l(s.name)} record from ${l(S(r.session.savedAt))}">×</button></div>`).join("")}</div></section>`).join("")}
    </div>
    <div class="dialog-actions"><button class="dialog-secondary" id="history-back">Back</button></div>`,o.querySelectorAll("[data-result-session]").forEach(n=>n.addEventListener("click",()=>V(n.dataset.resultSession,Number(n.dataset.resultTimer),e))),o.querySelectorAll("[data-delete-session]").forEach(n=>n.addEventListener("click",()=>pe(n.dataset.deleteSession,Number(n.dataset.deleteTimer),e))),o.querySelector("#history-back").addEventListener("click",N)}function pe(e,s,t){const a=i.sessions.find(r=>r.id===e),o=a?.timers?.[s];if(!a||!o)return;const n=document.querySelector("#dialog");n.innerHTML=`<h2>Delete ${l(o.name)} record?</h2><p>${S(a.savedAt)} · ${u(o.elapsed)} · ${b(o,a)}m</p><p class="delete-warning">This cannot be undone.</p><div class="dialog-actions"><button class="dialog-secondary" id="cancel-delete">Cancel</button><button class="dialog-danger" id="confirm-delete-record">Delete record</button></div>`,n.querySelector("#cancel-delete").addEventListener("click",()=>$(t)),n.querySelector("#confirm-delete-record").addEventListener("click",()=>{const r=i.sessions.findIndex(c=>c.id===e);r<0||(i.sessions[r].timers.splice(s,1),i.sessions[r].timers.length||i.sessions.splice(r,1),p(),E().some(c=>c.key===t)?$(t):N())})}function H(e){const s=i.sessions.find(a=>a.id===e);if(!s)return;const t=document.querySelector("#dialog");t.innerHTML=`<h2>${S(s.savedAt)}</h2><p class="dialog-subtitle">${s.poolLength}m pool</p>
    <div class="saved-swimmers">
      ${s.timers.map((a,o)=>`<button class="saved-swimmer" data-saved-swimmer="${o}"><strong>${l(a.name)}</strong><span>${u(a.elapsed)}</span><small>${a.laps.length} laps · ${b(a,s)}m</small></button>`).join("")}
    </div>
    <div class="dialog-actions"><button class="dialog-secondary" id="session-back">Back</button></div>`,t.querySelectorAll("[data-saved-swimmer]").forEach(a=>a.addEventListener("click",()=>V(e,Number(a.dataset.savedSwimmer)))),t.querySelector("#session-back").addEventListener("click",D)}function V(e,s,t=null){const a=i.sessions.find(c=>c.id===e),o=a?.timers?.[s];if(!a||!o)return;const n=document.querySelector("#dialog"),r=[o.stroke,o.test,o.equipment].filter(Boolean);n.innerHTML=`<h2>${l(o.name)}</h2><div class="record-hero"><strong>${b(o,a)}m</strong><b>${u(o.elapsed)}</b></div><p class="dialog-subtitle">${S(a.savedAt)} · ${o.laps.length} laps${r.length?` · ${r.map(l).join(" · ")}`:""}</p>${o.notes?`<p class="record-notes">${l(o.notes)}</p>`:""}
    <div class="lap-list" role="list">
      ${o.laps.length?o.laps.map((c,d)=>`<div class="lap-row" role="listitem"><strong>Lap ${d+1}<small>${(d+1)*a.poolLength}m</small></strong><span><small>Lap time</small><b>${u(c.split)}</b></span><span><small>Total</small><b>${u(c.at)}</b></span></div>`).join(""):'<p class="empty-state">No laps recorded.</p>'}
    </div>
    <div class="dialog-actions"><button class="dialog-secondary" id="swimmer-back">Back</button></div>`,n.querySelector("#swimmer-back").addEventListener("click",()=>t?$(t):H(e))}async function x(){if(!(!i.settings.keepAwake||!("wakeLock"in navigator)||L))try{L=await navigator.wakeLock.request("screen")}catch{}}async function w(){try{await L?.release()}catch{}L=null}document.addEventListener("visibilitychange",()=>{document.visibilityState==="visible"&&i.timers.some(e=>e.status==="running")&&x()});"serviceWorker"in navigator&&window.addEventListener("load",()=>navigator.serviceWorker.register("./service-worker.js"));m();
